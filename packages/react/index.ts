// packages/react/index.ts
import { createElement } from './src/createElement'

// Это наш публичный API, который будут использовать разработчики
const React = {
	createElement,
}

export default React
